// This file is auto-generated by @hey-api/openapi-ts

import { type DefaultError, queryOptions, type UseMutationOptions } from '@tanstack/react-query';

import { client } from '../client.gen';
import { Batch, Cache, Cluster, Meta, type Options } from '../sdk.gen';
import type { BatchDeleteData, BatchDeleteError, BatchDeleteResponse2, BatchGetData, BatchGetError, BatchGetResponse2, BatchPutData, BatchPutError, BatchPutResponse2, DeleteCacheItemData, DeleteCacheItemError, DeleteCacheItemResponse, GetCacheItemData, GetCacheItemError, GetCacheItemResponse, GetHealthzData, GetHealthzResponse, GetIdentityData, GetIdentityError, GetIdentityResponse, GetKeyOwnersData, GetKeyOwnersError, GetKeyOwnersResponse, GetOpenApiSpecData, GetOpenApiSpecResponse, PutCacheItemData, PutCacheItemError, PutCacheItemResponse } from '../types.gen';

export type QueryKey<TOptions extends Options> = [
    Pick<TOptions, 'baseUrl' | 'body' | 'headers' | 'path' | 'query'> & {
        _id: string;
        _infinite?: boolean;
        tags?: ReadonlyArray<string>;
    }
];

const createQueryKey = <TOptions extends Options>(id: string, options?: TOptions, infinite?: boolean, tags?: ReadonlyArray<string>): [
    QueryKey<TOptions>[0]
] => {
    const params: QueryKey<TOptions>[0] = { _id: id, baseUrl: options?.baseUrl || (options?.client ?? client).getConfig().baseUrl } as QueryKey<TOptions>[0];
    if (infinite) {
        params._infinite = infinite;
    }
    if (tags) {
        params.tags = tags;
    }
    if (options?.body) {
        params.body = options.body;
    }
    if (options?.headers) {
        params.headers = options.headers;
    }
    if (options?.path) {
        params.path = options.path;
    }
    if (options?.query) {
        params.query = options.query;
    }
    return [params];
};

export const getHealthzQueryKey = (options?: Options<GetHealthzData>) => createQueryKey('getHealthz', options);

/**
 * Liveness probe.
 *
 * Returns `200 ok` when the binary is running. Auth-free
 * regardless of `HYPERCACHE_AUTH_TOKEN` so external probes
 * (k8s, LBs) can reach it without credentials.
 *
 */
export const getHealthzOptions = (options?: Options<GetHealthzData>) => queryOptions<GetHealthzResponse, DefaultError, GetHealthzResponse, ReturnType<typeof getHealthzQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await Meta.getHealthz({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getHealthzQueryKey(options)
});

export const getOpenApiSpecQueryKey = (options?: Options<GetOpenApiSpecData>) => createQueryKey('getOpenApiSpec', options);

/**
 * This OpenAPI specification.
 *
 * Returns the embedded spec document so clients can discover
 * the API surface programmatically. Same content as the file
 * in this repo at `cmd/hypercache-server/openapi.yaml`.
 *
 */
export const getOpenApiSpecOptions = (options?: Options<GetOpenApiSpecData>) => queryOptions<GetOpenApiSpecResponse, DefaultError, GetOpenApiSpecResponse, ReturnType<typeof getOpenApiSpecQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await Meta.getOpenApiSpec({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getOpenApiSpecQueryKey(options)
});

/**
 * Remove a value.
 *
 * Idempotent — deleting a missing key returns 200 with
 * `deleted: true`. The `owners` list reflects the ring-owners
 * that should have held the key, useful for follow-up
 * verification.
 *
 */
export const deleteCacheItemMutation = (options?: Partial<Options<DeleteCacheItemData>>): UseMutationOptions<DeleteCacheItemResponse, DeleteCacheItemError, Options<DeleteCacheItemData>> => {
    const mutationOptions: UseMutationOptions<DeleteCacheItemResponse, DeleteCacheItemError, Options<DeleteCacheItemData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await Cache.deleteCacheItem({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getCacheItemQueryKey = (options: Options<GetCacheItemData>) => createQueryKey('getCacheItem', options);

/**
 * Retrieve a value.
 *
 * Default: raw value bytes (`application/octet-stream`) for
 * binary fidelity. Send `Accept: application/json` to get an
 * `ItemEnvelope` with TTL, version, owners, and a base64 value.
 *
 */
export const getCacheItemOptions = (options: Options<GetCacheItemData>) => queryOptions<GetCacheItemResponse, GetCacheItemError, GetCacheItemResponse, ReturnType<typeof getCacheItemQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await Cache.getCacheItem({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getCacheItemQueryKey(options)
});

/**
 * Store a value.
 *
 * Body is the raw value (any bytes; JSON, text, binary). The
 * cache stores it as `[]byte`. Use the `ttl` query parameter
 * for time-bounded entries.
 *
 */
export const putCacheItemMutation = (options?: Partial<Options<PutCacheItemData>>): UseMutationOptions<PutCacheItemResponse, PutCacheItemError, Options<PutCacheItemData>> => {
    const mutationOptions: UseMutationOptions<PutCacheItemResponse, PutCacheItemError, Options<PutCacheItemData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await Cache.putCacheItem({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

export const getKeyOwnersQueryKey = (options: Options<GetKeyOwnersData>) => createQueryKey('getKeyOwners', options);

/**
 * Resolve ring owners for a key.
 *
 * Pure visibility — returns the consistent-hash owners for the
 * given key without touching cache state. Works even when the
 * key has never been written.
 *
 */
export const getKeyOwnersOptions = (options: Options<GetKeyOwnersData>) => queryOptions<GetKeyOwnersResponse, GetKeyOwnersError, GetKeyOwnersResponse, ReturnType<typeof getKeyOwnersQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await Cluster.getKeyOwners({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getKeyOwnersQueryKey(options)
});

export const getIdentityQueryKey = (options?: Options<GetIdentityData>) => createQueryKey('getIdentity', options);

/**
 * Resolved caller identity.
 *
 * Returns the identity resolved from the request credentials
 * (bearer token, HTTP Basic, mTLS cert, OIDC JWT, or
 * `anonymous` when AllowAnonymous is enabled). Includes the
 * granted scopes and the derived capability strings so callers
 * can introspect their permissions without trial-and-error
 * against scope-protected routes.
 *
 * Requires the `read` scope — operators in pure-write or pure-
 * admin token configurations do not need to introspect their
 * own identity for normal cache use; the monitor's login flow
 * and the Go client SDK are the primary consumers.
 *
 */
export const getIdentityOptions = (options?: Options<GetIdentityData>) => queryOptions<GetIdentityResponse, GetIdentityError, GetIdentityResponse, ReturnType<typeof getIdentityQueryKey>>({
    queryFn: async ({ queryKey, signal }) => {
        const { data } = await Meta.getIdentity({
            ...options,
            ...queryKey[0],
            signal,
            throwOnError: true
        });
        return data;
    },
    queryKey: getIdentityQueryKey(options)
});

/**
 * Bulk fetch.
 *
 * Each requested key is looked up independently — a missing key
 * produces `{found: false}` without failing the whole batch.
 * Found entries carry the same shape as a single-key
 * `Accept: application/json` GET.
 *
 */
export const batchGetMutation = (options?: Partial<Options<BatchGetData>>): UseMutationOptions<BatchGetResponse2, BatchGetError, Options<BatchGetData>> => {
    const mutationOptions: UseMutationOptions<BatchGetResponse2, BatchGetError, Options<BatchGetData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await Batch.batchGet({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Bulk store.
 *
 * Each item's `value_encoding` selects how the wire `value`
 * string is interpreted: `base64` decodes bytes-first;
 * anything else (default) treats the string as UTF-8 text and
 * stores the raw bytes. Per-item errors are surfaced —
 * a single failure doesn't void the whole batch.
 *
 */
export const batchPutMutation = (options?: Partial<Options<BatchPutData>>): UseMutationOptions<BatchPutResponse2, BatchPutError, Options<BatchPutData>> => {
    const mutationOptions: UseMutationOptions<BatchPutResponse2, BatchPutError, Options<BatchPutData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await Batch.batchPut({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};

/**
 * Bulk delete.
 *
 * Same per-item semantics as bulk-put. Idempotent — keys that
 * never existed return `deleted: true`.
 *
 */
export const batchDeleteMutation = (options?: Partial<Options<BatchDeleteData>>): UseMutationOptions<BatchDeleteResponse2, BatchDeleteError, Options<BatchDeleteData>> => {
    const mutationOptions: UseMutationOptions<BatchDeleteResponse2, BatchDeleteError, Options<BatchDeleteData>> = {
        mutationFn: async (fnOptions) => {
            const { data } = await Batch.batchDelete({
                ...options,
                ...fnOptions,
                throwOnError: true
            });
            return data;
        }
    };
    return mutationOptions;
};
