// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { BatchDeleteData, BatchDeleteErrors, BatchDeleteResponses, BatchGetData, BatchGetErrors, BatchGetResponses, BatchPutData, BatchPutErrors, BatchPutResponses, DeleteCacheItemData, DeleteCacheItemErrors, DeleteCacheItemResponses, GetCacheItemData, GetCacheItemErrors, GetCacheItemResponses, GetHealthzData, GetHealthzResponses, GetIdentityData, GetIdentityErrors, GetIdentityResponses, GetKeyOwnersData, GetKeyOwnersErrors, GetKeyOwnersResponses, GetOpenApiSpecData, GetOpenApiSpecResponses, HeadCacheItemData, HeadCacheItemErrors, HeadCacheItemResponses, PutCacheItemData, PutCacheItemErrors, PutCacheItemResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

export class Meta {
    /**
     * Liveness probe.
     *
     * Returns `200 ok` when the binary is running. Auth-free
     * regardless of `HYPERCACHE_AUTH_TOKEN` so external probes
     * (k8s, LBs) can reach it without credentials.
     *
     */
    public static getHealthz<ThrowOnError extends boolean = false>(options?: Options<GetHealthzData, ThrowOnError>) {
        return (options?.client ?? client).get<GetHealthzResponses, unknown, ThrowOnError>({ url: '/healthz', ...options });
    }

    /**
     * This OpenAPI specification.
     *
     * Returns the embedded spec document so clients can discover
     * the API surface programmatically. Same content as the file
     * in this repo at `cmd/hypercache-server/openapi.yaml`.
     *
     */
    public static getOpenApiSpec<ThrowOnError extends boolean = false>(options?: Options<GetOpenApiSpecData, ThrowOnError>) {
        return (options?.client ?? client).get<GetOpenApiSpecResponses, unknown, ThrowOnError>({ url: '/v1/openapi.yaml', ...options });
    }

    /**
     * Resolved caller identity.
     *
     * Returns the identity resolved from the request credentials
     * (bearer token, mTLS cert, or `anonymous` when AllowAnonymous
     * is enabled). Includes the granted scopes so callers can
     * introspect their permissions without trial-and-error against
     * scope-protected routes.
     *
     * Requires the `read` scope — operators in pure-write or pure-
     * admin token configurations do not need to introspect their
     * own identity for normal cache use; the monitor's login flow
     * is the primary consumer.
     *
     */
    public static getIdentity<ThrowOnError extends boolean = false>(options?: Options<GetIdentityData, ThrowOnError>) {
        return (options?.client ?? client).get<GetIdentityResponses, GetIdentityErrors, ThrowOnError>({
            security: [{ scheme: 'bearer', type: 'http' }],
            url: '/v1/me',
            ...options
        });
    }
}

export class Cache {
    /**
     * Remove a value.
     *
     * Idempotent — deleting a missing key returns 200 with
     * `deleted: true`. The `owners` list reflects the ring-owners
     * that should have held the key, useful for follow-up
     * verification.
     *
     */
    public static deleteCacheItem<ThrowOnError extends boolean = false>(options: Options<DeleteCacheItemData, ThrowOnError>) {
        return (options.client ?? client).delete<DeleteCacheItemResponses, DeleteCacheItemErrors, ThrowOnError>({
            security: [{ scheme: 'bearer', type: 'http' }],
            url: '/v1/cache/{key}',
            ...options
        });
    }

    /**
     * Retrieve a value.
     *
     * Default: raw value bytes (`application/octet-stream`) for
     * binary fidelity. Send `Accept: application/json` to get an
     * `ItemEnvelope` with TTL, version, owners, and a base64 value.
     *
     */
    public static getCacheItem<ThrowOnError extends boolean = false>(options: Options<GetCacheItemData, ThrowOnError>) {
        return (options.client ?? client).get<GetCacheItemResponses, GetCacheItemErrors, ThrowOnError>({
            security: [{ scheme: 'bearer', type: 'http' }],
            url: '/v1/cache/{key}',
            ...options
        });
    }

    /**
     * Inspect metadata without fetching the value.
     *
     * Returns the same metadata as the `Accept: application/json`
     * GET, but in `X-Cache-*` response headers and with no body.
     * Cheap for cache-revalidation flows that just need to know
     * whether the key is current.
     *
     */
    public static headCacheItem<ThrowOnError extends boolean = false>(options: Options<HeadCacheItemData, ThrowOnError>) {
        return (options.client ?? client).head<HeadCacheItemResponses, HeadCacheItemErrors, ThrowOnError>({
            security: [{ scheme: 'bearer', type: 'http' }],
            url: '/v1/cache/{key}',
            ...options
        });
    }

    /**
     * Store a value.
     *
     * Body is the raw value (any bytes; JSON, text, binary). The
     * cache stores it as `[]byte`. Use the `ttl` query parameter
     * for time-bounded entries.
     *
     */
    public static putCacheItem<ThrowOnError extends boolean = false>(options: Options<PutCacheItemData, ThrowOnError>) {
        return (options.client ?? client).put<PutCacheItemResponses, PutCacheItemErrors, ThrowOnError>({
            security: [{ scheme: 'bearer', type: 'http' }],
            url: '/v1/cache/{key}',
            ...options,
            headers: {
                'Content-Type': 'application/json',
                ...options.headers
            }
        });
    }
}

export class Cluster {
    /**
     * Resolve ring owners for a key.
     *
     * Pure visibility — returns the consistent-hash owners for the
     * given key without touching cache state. Works even when the
     * key has never been written.
     *
     */
    public static getKeyOwners<ThrowOnError extends boolean = false>(options: Options<GetKeyOwnersData, ThrowOnError>) {
        return (options.client ?? client).get<GetKeyOwnersResponses, GetKeyOwnersErrors, ThrowOnError>({
            security: [{ scheme: 'bearer', type: 'http' }],
            url: '/v1/owners/{key}',
            ...options
        });
    }
}

export class Batch {
    /**
     * Bulk fetch.
     *
     * Each requested key is looked up independently — a missing key
     * produces `{found: false}` without failing the whole batch.
     * Found entries carry the same shape as a single-key
     * `Accept: application/json` GET.
     *
     */
    public static batchGet<ThrowOnError extends boolean = false>(options: Options<BatchGetData, ThrowOnError>) {
        return (options.client ?? client).post<BatchGetResponses, BatchGetErrors, ThrowOnError>({
            security: [{ scheme: 'bearer', type: 'http' }],
            url: '/v1/cache/batch/get',
            ...options,
            headers: {
                'Content-Type': 'application/json',
                ...options.headers
            }
        });
    }

    /**
     * Bulk store.
     *
     * Each item's `value_encoding` selects how the wire `value`
     * string is interpreted: `base64` decodes bytes-first;
     * anything else (default) treats the string as UTF-8 text and
     * stores the raw bytes. Per-item errors are surfaced —
     * a single failure doesn't void the whole batch.
     *
     */
    public static batchPut<ThrowOnError extends boolean = false>(options: Options<BatchPutData, ThrowOnError>) {
        return (options.client ?? client).post<BatchPutResponses, BatchPutErrors, ThrowOnError>({
            security: [{ scheme: 'bearer', type: 'http' }],
            url: '/v1/cache/batch/put',
            ...options,
            headers: {
                'Content-Type': 'application/json',
                ...options.headers
            }
        });
    }

    /**
     * Bulk delete.
     *
     * Same per-item semantics as bulk-put. Idempotent — keys that
     * never existed return `deleted: true`.
     *
     */
    public static batchDelete<ThrowOnError extends boolean = false>(options: Options<BatchDeleteData, ThrowOnError>) {
        return (options.client ?? client).post<BatchDeleteResponses, BatchDeleteErrors, ThrowOnError>({
            security: [{ scheme: 'bearer', type: 'http' }],
            url: '/v1/cache/batch/delete',
            ...options,
            headers: {
                'Content-Type': 'application/json',
                ...options.headers
            }
        });
    }
}
