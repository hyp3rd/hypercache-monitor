// Test setup runs before every test file. Jest-DOM matchers
// (`toBeInTheDocument`, `toHaveClass`, etc.) need to be registered
// here so React Testing Library tests get the assertion vocabulary.
import "@testing-library/jest-dom/vitest";
